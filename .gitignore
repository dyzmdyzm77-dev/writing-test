# Dependencies
node_modules/

# Build output
# code.js는 빌드 산출물이지만 일부러 커밋한다 — 회사 PC↔맥을 git으로 오갈 때
# pull 직후 빌드 없이 플러그인이 바로 돌아야 하기 때문 (npm 없는 PC 폴백도 동일)
out/
dist/

# Logs
*.log
*.log.*

# sync-feedback가 만드는 검토용 임시 파일 (커밋 금지 — 내용은 recommend-examples.md로 옮겨서 반영)
feedback-candidates.md

# Claude Code 로컬 설정 (PC별 임시 경로·개인 권한 — 커밋 금지)
.claude/

# macOS
.DS_Store
