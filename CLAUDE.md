# S-1 S/W UX Writing Agent (Figma 플러그인)

에스원 UX Writing 검토 플러그인. 선택 영역의 텍스트를 네이버 맞춤법 + 로컬 규칙(용어집/해요체/띄어쓰기)으로 검사하고, 캔버스에 형광펜·코멘트를 표시한다.

## 빌드/실행

- `npm install` (최초 1회) → `npm run build` → Figma에서 Plugins > Development > Import plugin from manifest
- `npm run build` = glossary.md를 code.ts에 주입(`scripts/build-glossary.js`) + tsc 컴파일. **code.ts의 GLOSSARY:BEGIN/END 마커 영역은 자동 생성 — 직접 수정 금지**
- ui.html은 빌드 불필요 (플러그인 재실행만)
- 검증은 vm 샌드박스로: code.js를 `vm.runInContext`에 figma 스텁과 함께 로드해 `suggestFriendlyKorean(text, naverChecked)` 호출

## 용어집 워크플로우

- 단순 치환/합성어 보호/동작 명사/예외 표기는 **glossary.md에서 편집** → `npm run build`
- 가드(lookahead)가 필요한 규칙만 code.ts의 TERM_RULES에 직접 작성
- "X → X처럼 똑같아 보이는 제안"이 나오면 → 네이버가 합성어를 띄어 쓴 것 → glossary.md "합성어 보호"에 단어 추가
- 네이버가 표기를 바꾸는 단어(렌탈→렌털 등) → "예외 표기" 표에 추가

## 피드백 환류 워크플로우

- **좋아요는 다듬기·대화 두 모드 다 있어야 한다** (2026-07: 대화 모드 카드에 복사만 있고 좋아요가 빠져 있던 것을 수정). 대화 모드엔 '원본 문구'가 없어서 `before`로 **그 제안을 부른 사용자 요청 메시지**를 보낸다 — sync-feedback.js가 before를 기준으로 묶고 **비어 있으면 후보에서 통째로 버리므로** 빈 값을 보내면 안 된다. 카드 구조·좋아요 키 구분자(`'\\u0001'`)·wireLikeButtons는 renderRecommend와 공유 — 한쪽만 바꾸면 어긋난다
- 추천 카드의 좋아요(👍)는 제보 저장소(report-admin)에 `reason='추천 좋아요'` 마커로 저장됨 — 이 문자열은 code.ts(LIKE_SUGGESTION)와 scripts/sync-feedback.js가 공유하므로 한쪽만 바꾸면 안 됨
- `npm run sync-feedback` → 좋아요+오수정 제보를 내려받아 `feedback-candidates.md` 생성(커밋 금지, gitignore됨) → 사람이 골라 recommend-examples.md/ux-writing.md에 옮기고 `npm run build`
- 사내 프록시 때문에 node fetch에 `--use-env-proxy` 필요 (npm 스크립트에 포함됨, Node 24+)

## 핵심 설계 결정 (바꾸기 전에 읽을 것)

- **띄어쓰기는 네이버 우선**: 부사 띄어쓰기 정규식(ADVERB_SPACING_RULES)은 네이버 실패 시 폴백 전용. COMPOUND_PROTECT_RULES가 네이버의 합성어 분리를 되돌림
- **네이버 검사는 배치**(여러 문구를 \n으로 묶어 요청 1개) + 세션 캐시(naverCache). 줄바꿈은 <br>로 보존됨(실서버 확인). 공백 구조는 alignWhitespace로 원문에 정렬 (U+2028 등 모든 줄바꿈 문자 처리 — \n만 가정하지 말 것)
- **위치 추적은 폴링 금지**: dynamic-page라 figma.on('documentchange')는 등록 실패 → `figma.currentPage.on('nodechange')` 사용. setInterval 폴링은 수천 개 어노테이션에서 캔버스 렉 유발 (제거된 이력 있음)
- **동기 getNodeById 금지** (dynamic-page에서 throw) → getNodeByIdAsync
- '해 주세요' 띄어쓰기: 동작 명사(glossary.md 목록)면 '확인해 주세요', 부사면 '같이 해주세요'
- 검토 사유는 키워드 칩(맞춤법/띄어쓰기/해요체/간결하게/용어 통일 등 10종)으로 표시
- **추천·번역은 클로드 다리 전용** (2026-07 Gemini/API 키 경로 전부 제거 — 키 모달·사용량 카운터·serverSharedKeyMissing 없음). 추천: 다리 `/recommend`, 다리가 꺼져 있으면 예시·규칙 폴백. 번역: 다리 `/translate`(같은 세션, 턴별 지시로 JSON 객체 {translated, direction} 요구 — 줄 수 유지 지시 포함), 다리 없으면 토스트 안내만. 추천·번역 화면 진입 클릭(홈카드·상단탭)이 **제스처 안에서** 다리를 자동으로 켠다(ensureBridgeFromGesture — 비동기 응답 뒤 window.open은 팝업 차단됨). 예시 사전은 화면에 안 나오고 AI few-shot 교재로만 쓰임. 예시 사전의 추천안은 검토 규칙과 모순되면 안 됨(마침표·'해 주세요' 띄어쓰기 포함) — vm 샌드박스로 suggestFriendlyKorean(추천안)이 무변경인지 확인
- **클로드 다리(scripts/claude-bridge.js)**: `claude -p` stream-json 세션을 상시 유지하는 로컬 서버(localhost:11888). 켜기/끄기는 자동 — 감시자가 켜고 플러그인 하트비트로 끈다(수동 시작은 npm run bridge, 종료는 POST /shutdown). (수동 런처 클로드다리-켜기/끄기.bat은 2026-07 삭제 — 자동화가 대체) (클로드다리-자동시작등록.vbs는 제거됨 — 다리 자체를 로그인마다 자동 기동하는 구버전 방식으로, 감시자 온디맨드 설계·비용 원칙과 충돌) **깨우기 주경로는 감시자(scripts/bridge-watcher.js, 포트 11889)**: 피그마 데스크톱이 플러그인의 claudebridge:// 열기를 window.open/숨은 iframe/figma.openExternal **셋 다 소리 없이 막는 것이 실측 확인됨**(2026-07, 4분 프로세스 감시로 wscript 도달 0회) — 유일하게 안 막히는 통로는 fetch. 그래서 초소형 상주 감시자(~15MB, claude 안 물어 클로드 앱 업데이트 안 막음)가 POST /wake를 받으면 다리를 켠다. 로그인 자동시작(HKCU Run, ClaudeBridgeWatcher)으로 등록, 사용자 승인 받음. WAKE_BRIDGE는 **순차 폴백**(2026-07 병행→순차 전환): 감시자 fetch 성공 시 거기서 끝, 실패했을 때만 openExternal(claudebridge://) 보조 — 병행하면 프로토콜이 안 막힌 피그마에서 다리가 이중 기동돼 창이 보일 수 있었음. 같은 이유로 ui.html의 window.open/숨은 iframe 프로토콜 열기도 제거(openBridgeProtocol은 WAKE_BRIDGE 위임만). **창 안 뜨게 하는 규칙(win32)**: 감시자의 다리 스폰은 node 직접 + windowsHide(CREATE_NO_WINDOW), detached 금지 — cmd/vbs 경유·detached+windowsHide 조합은 콘솔 창이 사용자에게 노출됨(실측). 숨은 콘솔을 claude 자식이 물려받아 어떤 창도 안 생긴다. 감시자 /health의 v 필드로 구버전 프로세스 잔존 확인(v2=창숨김판). manifest devAllowedDomains에 11889 추가됨(manifest 변경은 플러그인 재실행 필요). **첫 PC 설정은 npm이 자동으로 해결**: `npm install`/`npm run build`가 scripts/register-protocol.js를 실행해 claudebridge:// 등록 + 감시자 자동시작 등록 + 감시자 즉시 기동(관리자 불필요, fail-soft). **macOS도 지원(2026-07)**: register-protocol.js가 darwin이면 launchd LaunchAgent(com.claudebridge.watcher)로 감시자만 등록한다 — claudebridge://는 등록 안 함(피그마가 프로토콜을 막아 감시자 fetch가 유일 경로라 불필요). plist에 등록 시점 PATH를 굳혀 넣는 것이 핵심(launchd 기본 PATH엔 /usr/local/bin이 없어 claude를 못 찾음). 감시자의 다리 스폰은 전 OS에서 process.execPath 직접 스폰 (win32는 windowsHide, 상세 규칙은 위 WAKE_BRIDGE 항목). killProc은 POSIX에서 detached 스폰 + 프로세스 그룹 kill(-pid)로 claude 트리를 정리(taskkill /T 대응 — 2026-07 Mac에서 고아 0 실측). 추천·번역은 개인 클로드 구독 전용이 확정 노선 — 서버 API 키·Gemini 경로 재도입 금지. npm 없이 빌드된 폴더만 복사받은 PC를 위한 폴백: 켜기 버튼이 20초 안에 실패하면 버튼이 [🔧 클로드 설치 파일 받기]로 바뀌고, 누르면 **클로드-커넥터.bat**을 다운로드로 내려준다(GET_INSTALLER → installer-file, code.ts INSTALLER:BEGIN/END 마커에 build가 base64 주입). 이 bat은 자기완결형(다리 js+예시 md+런처 vbs를 base64 payload로 내장, `exit /b` 뒤 ::PS::/::BRIDGE::/::EXAMPLES::/::LAUNCHER::/::END:: 섹션): 실행하면 %LOCALAPPDATA%\ClaudeBridge에 설치 + claudebridge:// 등록 + node/claude 점검(없으면 단계별 팝업: nodejs.org 열기 / claude 설치·로그인 안내) + 다리 즉시 켜기까지 전부 한다. CB_SILENT=1이면 팝업 생략(자동 테스트용). bat은 npm run build가 저장소 루트에 생성 — 직접 수정 금지. **프로토콜 등록만큼은 플러그인 버튼이 직접 못 함**(피그마 샌드박스 — 레지스트리/실행 능력 없음, 우회로 없음) — 그래서 "다운로드 → 한 번 실행" 흐름이 최소치. 저장소 폴더가 있는 PC는 npm install 한 번으로 동일하게 등록된다(원클릭연결.vbs는 2026-07 삭제 — npm install이 대체). claudebridge://는 claude-bridge-silent.vbs(점검 런처)를 실행. 두 vbs와 설치 PS의 node/claude 점검 로직은 세쌍둥이 — 같이 유지. vbs는 반드시 UTF-16LE로 저장([[vbs-must-be-utf16le]]). 속도 핵심 3가지 — 세션 재사용(시동 1회), **빈 폴더 cwd**(저장소에서 실행하면 프로젝트 맥락 로딩으로 45초/턴), MAX_THINKING_TOKENS=0 + CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 (→ 약 3초/턴). 같은 문구 재요청이면 "이전과 다른 제안" 지시(askedCount). 모델은 opus 기본·권장(ui.html getSelectedModel()이 'opus' 상수 반환, 다리 기본값 CLAUDE_MODEL도 opus). 속도 위해 sonnet 전환을 검토했으나 opus 유지로 결정(2026-07). 다리는 요청의 model이 바뀌면 세션 재시작(재워밍업 ~18초, 같은 모델은 5~10초). 중복 실행 시 EADDRINUSE 감지해 새 인스턴스가 조용히 종료. **다리 견고화(2026-07, BRIDGE_V=18)**: ① 콘솔 로그를 임시폴더 claude-bridge.log에도 남김(감시자 스폰이 stdio ignore라 이게 유일한 사후 추적로, 2MB 넘으면 .old 한 세대) ② 턴 도중 claude 프로세스가 죽으면(SESSION_DIED) 1회 자동 재시도 — 단 **의도적 종료와 구분할 것**: /open-login·/claude-logout의 killProc은 reason을 줘서 재시도가 옛 계정 세션을 되살리지 않게 한다(계정 전환 직후 옛 자격증명 부활 버그의 원인) ③ 응답이 JSON 형식을 벗어나면(간헐 실측: "클로드 응답을 해석하지 못했어요") 같은 세션에 "형식대로 다시" 1회 재요청 — **반드시 runTurn의 reparse 옵션으로 같은 큐 잡 안에서**: 별도 잡으로 빼면 동시 요청 턴이 끼어 남의 답을 재출력받고(내용 오염), MAX_TURNS 경계에서 세션이 재시작돼 '방금 답' 없는 새 세션이 지어낸다 ④ 타임아웃·재시도는 시간 예산 가드(플러그인 130초 제한 초과 방지). 오류 문구 접두어는 다리(friendlyError)만 붙인다 — code.ts는 data.error를 BRIDGE_GUIDE 마커로 그대로 노출("클로드 호출 실패:"가 이중으로 겹치던 실측 버그). **구버전 다리 감지(2026-07)**: 코드를 pull·복사해도 **이미 떠 있는 다리는 옛 코드 그대로**라 껐다 켜기 전엔 새 동작이 안 나온다(맥만 고쳐진 것처럼 보이는 착시의 원인 — 실제 발생). 그래서 /health가 v(BRIDGE_V)·dir(__dirname)을 노출하고, code.ts의 BRIDGE_MIN_V보다 낮거나 v가 없으면 problem='bridge-old' → 버튼 [🟠 업데이트 필요] → 클릭 시 RESTART_BRIDGE(=/shutdown → 감시자 /wake → 최대 9초 폴링)로 새 코드로 재시작. **동작이 바뀌는 수정을 하면 BRIDGE_V와 BRIDGE_MIN_V를 함께 올릴 것.** 재시작 후에도 bridge-old면 감시자가 다른 폴더(설치본 %LOCALAPPDATA%\ClaudeBridge 등)의 다리를 켜는 것이므로 dir 경로를 토스트로 알려준다. **업데이트는 화면 진입 때 자동(2026-08, 사용자 결정)**: bridge-old인데 버튼을 찾아 누르게 할 이유가 없다(새 버전이어야 추천이 되니 물어볼 결정이 아님) → ui.html `restartBridgeForUpdate(auto)`를 ensureBridgeFromGesture(추천·번역 진입)와 bridge-status(추천 화면에 머무는 중 옛 버전이 잡힐 때) 양쪽에서 자동 호출하고 덮개를 'waking'으로 세운다. 주의 3가지: ① 재시작엔 켜기 폴링이 없어 **끝을 알리는 신호가 필요**하다 — code.ts가 `bridge-restart-done`(ok/alive/problem)을 보내야 덮개가 걷힌다(없으면 '연결하는 중'이 영영 돈다) ② `bridgeRestarting` 동안 bridge-status의 자동 깨우기(`!alive`)를 건너뛴다 — 껐다 켜는 중간의 '꺼짐'을 보고 또 깨우면 이중 기동 ③ 실패는 `bridgeRestartFailed`로 기억해 **진입마다 다리를 껐다 켜지 않는다**(다른 폴더의 다리인 경우 무한 재시작) — 그 뒤엔 [업데이트 필요] 버튼이 남고, 직접 누르면 플래그를 풀고 다시 시도한다. **사용 한도 초과 감지(2026-08, BRIDGE_V=23)**: 로그인은 멀쩡한데 "더 못 쓴다"는 경우 — 실측 문구는 회사 엔터프라이즈 좌석의 `You've hit your individual spend limit · run /usage-credits to ask your admin for a higher limit`(관리자가 **사람별로** 걸어 둔 상한이라 조직 플랜 사용량이 남아도 걸린다 — 사용자가 "사용량 많이 남았는데 왜 안 되냐"고 신고한 원인). 이 케이스가 없던 탓에 영어 원문이 그대로 토스트돼 무엇을 해야 하는지 알 수 없었다 → `isLimitError`로 잡아 `claudeStatus='claude-limit'` + LIMIT_GUIDE 한국어 안내, 버튼은 주황 [🟠 클로드 한도 초과](bridge-warn 공유), 클릭은 `openClaudeLogin(true)`(**계정 선택 화면** — 같은 계정으로 재로그인하면 그대로 막히므로 claude-logout과 달리 switch=true). **정규식을 '한도'로 넓히지 말 것**: rate limit(잠깐 몰림)이나 문맥 길이 초과까지 잡으면 엉뚱하게 계정 전환을 안내하게 된다 — `spend limit|usage-credits|usage limit (reached|exceeded)`로 좁혀 두고 분류 테스트 8건으로 지킨다. 결과 핸들러에서 **isAuthError보다 먼저** 검사한다(인증 정규식이 `log ?in` 등으로 넓어 문구가 바뀌면 삼킬 수 있음). retryAuthIfNeeded도 claude-limit을 포함해 한도가 풀리면 자동으로 🟢 복귀(한도에 걸린 호출은 거절되므로 사용량은 안 나간다). **다만 안내만으로는 못 풀리는 짝 문제가 있었다 — 계정을 바꿔도 안 갈아타는 것(2026-08, BRIDGE_V=26)**: 다리는 `claude -p` 세션을 길게 유지하고 **시동할 때 받은 입장권을 계속 쓴다**. 그래서 터미널·브라우저에서 다른 계정으로 로그인해도 이미 떠 있는 세션은 옛 계정으로 계속 호출해, 새 계정에 사용량이 남아 있어도 "한도 초과"가 그대로 나온다(실측 신고: "새 계정으로 로그인했는데 왜 그 계정 사용량을 못 쓰냐" — 한도 안내만 고치고 이걸 안 고치면 사용자는 여전히 막힌다). 플러그인을 거친 로그인·로그아웃은 killProc으로 세션을 버려서 이 문제가 없었고 **밖에서 바꾼 경우만** 사각지대였다 → `startProc`이 `sessionAccount`(시동 시 계정)를 기록하고, `/health`마다 `restartIfAccountChanged()`가 파일의 계정과 비교해 다르면 세션을 버린다(claudeStatus=null로 한도·로그인 상태도 새 계정 기준으로 재판정). 비용 0 — 파일만 읽고 claudeAccount의 30초 캐시를 그대로 쓴다(.claude.json이 커서 매 조회 재읽기는 피한다). **'계정 있음 → 없음' 방향은 일부러 무시한다**: 파일을 덮어쓰는 순간 잠깐 못 읽는 것과 구분되지 않아 헛 재시작을 부르고, 그 방향은 인증 오류 경로가 이미 처리한다. retryAuthIfNeeded보다 **먼저** 불러야 워밍업이 옛 계정으로 돌지 않는다. 검증: 모의 홈 폴더의 .claude.json 계정을 바꿔 12초 만에 감지·세션 재시동(시동 2회) 확인. **로그인 필요 감지(2026-07)**: `claude --version`은 로그인 없이도 성공해서 시동 점검(checkClaudeAvailable)으로는 못 잡는다 — 실제 턴의 인증 오류(isAuthError)를 잡아 오류 문구를 한국어 안내(LOGIN_GUIDE)로 바꾸고 /health·오류 응답에 problem='claude-logout'을 실어 보낸다. 실측 오류 문구 2종을 모두 커버해야 함: 만료="Failed to authenticate: OAuth session expired…", 미로그인="Not logged in · Please run /login" (후자 때문에 정규식에 log ?in|logged 포함 — 좁히지 말 것). 오류 응답 변환은 friendlyError()가 담당(claude-missing이면 설치 안내로), code.ts는 problem이 실린 오류를 접두어 없이 그대로 토스트한다(BRIDGE_GUIDE: 마커). 성공 턴은 어떤 claudeStatus든 'ok'로 되돌린다(재설치 복귀 포함). **자동 복귀**: 로그인 창을 연 뒤 UI가 10초×12회 /health를 폴링하고, 다리의 retryAuthIfNeeded()가 claude-logout 상태의 /health 조회 때 30초에 1번 워밍업을 재시도 — 재로그인이 끝나면 사용자가 아무것도 안 눌러도 버튼이 🟢·새 계정으로 돌아온다(모의 claude로 e2e 검증됨). 테스트는 BRIDGE_PORT 환경변수로 별도 포트에서: 미로그인 재현=HOME·CLAUDE_CONFIG_DIR을 빈 폴더로, CLI 없음 재현=PATH에서 claude 제거(node만 있는 fakebin). ANTHROPIC_API_KEY 가짜값은 재현 안 됨(정상 OAuth가 우선). code.ts bridgeHealth는 응답의 ok/engine==='claude'를 확인해 11888을 점유한 다른 앱을 다리로 착각하지 않는다. 플러그인 버튼은 주황 [🟠 클로드 로그인 필요]로 표시(bridge-warn 클래스), 재로그인 후 성공 턴이 오면 자동으로 'ok' 복귀. code.ts는 추천·번역 실패 직후 refreshBridgeStatus()로 버튼을 갱신한다. **주황 버튼 클릭 → 브라우저 로그인 직행(2026-07 확정)**: OPEN_CLAUDE_LOGIN → 다리 POST /open-login이 기본으로 `claude auth login --claudeai`를 **숨은 프로세스**로 실행 — 1/2/3 메뉴 없이 곧장 브라우저가 열리고, CLI가 localhost LISTEN으로 결과를 자동 수령해 터미널이 화면에 전혀 안 뜬다(헤드리스에서도 브라우저 열림+LISTEN 실측). 응답 mode='browser'. 프로세스는 loginProc으로 추적(10분 타임아웃, exit 시 killLoginProc으로 트리 정리, 완료 시 accountCache 무효화). **로그인은 기본 브라우저로**(BROWSER 환경변수): claude CLI가 **BROWSER를 존중**해 브라우저를 직접 안 열고 writeBrowserHandler()가 만든 스크립트에 authorize URL을 넘긴다(실측 2026-07). redirect_uri가 localhost 콜백이라 붙여넣기 없이 자동 완료. **계정 전환 = 계정 선택 화면으로 바로(2026-07 최종, 사용자 요구)**: 승인 화면을 거치지 않고 계정 선택 화면으로 곧장 보낸다. URL로는 로그인된 계정을 못 바꾼다 — 로그인돼 있으면 authorize가 claude.ai/oauth/authorize(승인 화면)로 가고 selectAccount=true·prompt=select_account로도 못 뚫는다(실측). 승인 화면 하단에 [계정 전환] 버튼이 있지만 사용자가 "플러그인에서 눌렀으면 그 화면으로 바로 가야지"라고 요구 → switch 모드는 claude.ai/logout?returnTo=<url-encoded /oauth/authorize?QUERY(상대경로)>로 한 탭에서 로그아웃→login?selectAccount(계정 선택)로 잇는다(실측: 단일 탭, selectAccount 착지). 부작용: 브라우저 claude.ai 웹 로그인도 풀림(계정 전환 의도와 방향 같아 수용). 변환은 mac=node(process.execPath), win=PowerShell EscapeDataString. returnTo는 같은 도메인 상대경로만 따름(claude.com 전체 URL 안 됨). normal 모드(만료 재로그인)는 로그아웃 없이 그냥 연다. 히스토리(오간 결정): 로그아웃강제→2탭문제→단일탭→승인화면버튼에맡김(부작용 없앰)→**다시 단일탭 계정선택 직행**(사용자가 승인화면 거치는 걸 원치 않음). 시도해서 안 된 것: selectAccount=true/prompt=select_account 직접 씌우기(로그인 상태 못 뚫음), logout?returnTo=<claude.com 전체 URL>(외부 도메인이라 체이닝 안 됨). **동작 바꿨으니 BRIDGE_V=10, BRIDGE_MIN_V=10.** ⚠️ **위 단일탭 체이닝은 2026-08에 폐기됐다(BRIDGE_V=24)**: claude.ai가 `logout?returnTo=`의 **쿼리스트링을 떼어 버리게 바뀌어** 되돌아온 /oauth/authorize에 client_id가 없어졌다 → 브라우저에 "잘못된 OAuth 요청 · client_id 매개변수가 누락되었습니다"만 뜨고 **로그인 자체가 불가능**해졌다(실측 신고). **writeBrowserHandler(BROWSER 가로채기)는 아예 제거됐다(BRIDGE_V=25)** — 재작성이 없어지자 핸들러는 목적이 없어졌고, 남겨 두면 오히려 로그인을 망가뜨렸다: CLI가 URL을 따옴표 없이 넘기면 **cmd가 `&`에서 URL을 잘라** 뒤쪽 매개변수(client_id 등)가 사라지고, 심하면 **브라우저가 아예 안 열린다**(실측: CLI 프로세스는 대기 중인데 창이 안 뜸 — "브라우저가 안 뜬다" 신고의 원인). 이제 BROWSER를 건드리지 않고 **claude CLI가 기본 브라우저를 직접 연다**(CLI 기본 동작). ⚠️ **로그인 경로에서 BROWSER를 건드리면 안 된다(2026-08 실측 2회 확정, BRIDGE_V=35)**: BROWSER를 설정하면 — 내용이 무엇이든, **아무것도 안 하는 no-op이어도** — claude CLI가 브라우저 핸드오프를 포기하고 **"인증 코드를 Claude Code에 붙여넣으세요" 방식으로 바뀐다**. 다리는 로그인 프로세스를 stdin 없이 숨겨 띄우므로 붙여넣을 곳이 없어 **로그인이 아예 불가능해진다**(실측 신고 2회: "승인 누르니까 코드 붙여넣기 나온다"). localhost LISTEN이 떠 있는 것만 보고 자동 수령이 유지된다고 판단한 것이 오진이었다 — LISTEN 유무로는 확인되지 않는다. **그래서 "탭 1개 + 계정 선택 화면"은 이 CLI로 불가능하다**: 한 탭으로 이으려면 CLI의 열기를 막아야 하고(→코드 붙여넣기), 로그아웃을 따로 열면 탭이 2개가 된다. 시도해서 폐기한 것들: ① 시크릿 창(BRIDGE_V=27~29) — 브라우저를 우리가 골라야 해 기본 브라우저가 아닌 크롬·엣지가 열리고("왜 크롬으로 열려"), 기본 브라우저가 시크릿 인자를 조용히 무시하면 일반 창이 떠 승인 화면 그대로(삼성 인터넷 실측) ② 로그아웃 탭 먼저 열기(BRIDGE_V=31) — 계정 선택 화면까지 가지만 **탭 2개**(claude.ai/login?from=logout + login?selectAccount) ③ 한 탭 체이닝 `claude.ai/logout?returnTo=<url-encoded /oauth/authorize?QUERY>`(BRIDGE_V=33~34) — URL 조립은 맞았고(cmd 잘림을 피해 rundll32로 열면 client_id·state 보존, 단 **returnTo 경로에서 /cai를 떼야 한다** — claude.com은 claude.ai 콘텐츠를 /cai 아래로 라우팅하지만 로그아웃은 claude.ai로 착지해 그대로 넣으면 404) 그러나 CLI 열기를 막아야 해서 ①의 코드 붙여넣기에 걸렸다. **결론(사용자 결정): 탭 1개 + 승인 화면.** 계정 전환은 승인 화면 하단 [계정 전환] 버튼으로 한다. 교훈: **외부 사이트의 리다이렉트 규칙에 기대는 URL 가공은 예고 없이 깨진다** — 로그인 경로에 그런 의존을 다시 넣지 말 것. switchMode는 로그 문구·응답 mode 표시용으로만 남았다. **로그아웃(2026-07, BRIDGE_V=11)**: 홈 계정 줄(homeAccountBar)에 [로그아웃] 버튼 추가([계정 전환] 옆, 로그인된 계정 있을 때만 표시). LOGOUT_CLAUDE → 다리 POST /claude-logout이 `claude auth logout`(비대화형) 실행 → killProc + accountCache 무효화 + claudeStatus=null. code.ts는 성공 시 confirmedClaudeAccount도 비운다(재로그인하면 새로 확인받게). UI는 오클릭 방지로 2단계 확인("정말 로그아웃?" → 실행). 다리 꺼져 있으면 감시자로 깨워 재시도. **로그인이 안 됐으면 추천 화면을 미리 덮는다(2026-08, ui.html)**: 예전엔 로그아웃 상태로도 추천 화면에 들여보내서 [추천받기]를 누른 뒤에야 "로그인이 필요해요"로 실패했다(실측 신고: "로그인이 안 되면 처음부터 로그인 화면이 떠야 하지 않나") → bridgeLoadState에 `login` 상태 추가(stopped와 같은 규격: 예시 그래픽·액션 표시, 스피너·[닫기] 숨김 — 로그인 없이는 추천이 안 되니 닫을 수 없다). bridge-status에서 `alive && problem==='claude-logout' && currentView==='recommend'`면 세우고, 로그인되면(problem이 풀리면) 걷는다. 덮개 주 버튼은 라벨이 [클로드 로그인하기]로 바뀌고 클릭은 openClaudeLogin(false) — 다리는 이미 켜져 있으니 깨우기가 아니라 로그인만 필요하다. **로그인 대기 중 재클릭**: 원칙은 항상 브라우저 재시도. 터미널 폴백(mode='terminal', openLoginTerminal)은 **창을 아무것도 못 띄웠을 때만**(`loginWindowOpened===false` + 20초 경과) — 브라우저가 localhost에 못 닿아 코드가 보이는 환경용(코드 붙여넣기 가능한 창). ⚠️ 예전엔 '60초 넘게 대기 중이면 터미널'이었는데, 로그인 화면을 읽다가/잠깐 딴 일 하다 다시 누른 **정상적인 경우에도 cmd 창이 튀어나왔다**(2026-08 실측 신고: "터미널 화면은 왜 떠 갑자기" — 앞선 로그인이 2분 대기 중이었음). 이제 우리가 창을 직접 열고 성공 여부를 아니까 **시간이 아니라 그 사실로 판단한다**(BRIDGE_V=29). normal 모드는 CLI가 여는 걸 관찰할 수 없어 열린 것으로 본다 — false로 두면 재클릭 때 터미널이 뜬다. 폴백 창은 `claude /login`이 켜진 터미널을 연다(win32: start cmd /k + tmp의 claude-bridge-login.ps1이 5초 뒤 SendKeys 엔터로 1번(구독 계정) 자동 선택 후 창을 SW_MINIMIZE — 사용자 눈엔 브라우저만 남음. AppActivate/FindWindow 실패 시 아무것도 안 함(다른 창 오입력 방지, 메뉴 보인 채 남음). claude가 콘솔 제목을 바꾸면 못 찾을 수 있어 **윈도우 실기 검증 필요**. darwin: osascript Terminal + 6초 뒤 System Events keystroke return으로 자동 엔터 시도 — **손쉬운 사용(접근성) 권한이 있어야 작동**, 없으면 keystroke만 조용히 실패하고 사용자가 엔터 한 번(fail-soft, 실측: 무권한 환경에서 메뉴 유지 확인). 엔터 직전 Terminal 재활성화로 오입력 방지. expect(pty)로 보낸 키는 클로드 TUI가 무반응 — 일반 \r·kitty 프로토콜(\x1b[13u) 모두 실측 확인(2026-07), 메뉴 문구도 커서이동 코드 때문에 "Select login method" 연속 문자열로 스트림에 존재하지 않음(패턴 매칭 시 글롭 필수)). 브라우저는 1번 선택 시 CLI가 스스로 연다 — 다리가 터미널 없이 OAuth를 직접 수령하는 방식(공식 앱 사칭)은 약관·안정성 문제로 금지. **로그인 전 다리 보장(2026-07)**: 로그인 창을 여는 건 다리인데 계정 화면은 비용 때문에 다리를 안 켜둔다 → OPEN_CLAUDE_LOGIN이 곧장 /open-login을 치면 "다리 꺼짐?" 오류가 난다(실측). 그래서 code.ts OPEN_CLAUDE_LOGIN은 /open-login 실패 시 감시자 /wake(보조 claudebridge://)로 다리를 깨우고 최대 12초 폴링 후 재시도한다. UI(accountSwitch)의 자체 6초 대기 깨우기 로직은 제거 — code.ts가 일원화해서 처리. 플러그인 샌드박스는 프로그램 실행 불가라 다리가 대행. 이때 killProc으로 낡은 입장권을 문 대기 세션을 버려 재로그인 후 다음 요청이 새 세션으로 뜨게 한다. 브라우저 OAuth 로그인 자체는 본인 몫(자동화 불가·비밀번호 대행 금지). **계정 표시(2026-07)**: 다리가 ~/.claude.json의 oauthAccount.emailAddress를 읽어(30초 캐시, /open-login 시 캐시 무효화) /health의 account로 노출 → 버튼이 "🟢 클로드 켜짐 · <이메일 @앞>"으로 누구 구독으로 도는지 보여준다(전체 이메일은 title 툴팁·켜짐 토스트에). 공용 PC에서 남의 계정을 모르고 쓰는 일 방지용. 켜짐 상태에서의 계정 갈아타기는 [🔑] 버튼(recommendHeader, bridgeLoginBtn) — 같은 OPEN_CLAUDE_LOGIN 경로로 로그인 창을 열고, 이후 10초×12회 상태를 재확인해 계정 표시를 갱신한다. **첫 화면 = 계정 화면(2026-07)**: 플러그인을 켜면 accountView부터 — 이 PC에 로그인된 계정을 보여주고 [이 계정으로 시작]→홈 / [다른 계정으로 로그인] / [로그인 없이 검토만 쓰기]. 이미 확인한 계정과 같으면 건너뛰고 홈으로(decideFirstScreen — 매번 물으면 성가심). 홈 상단에도 계정 줄(homeAccountBar) 상시 표시. **계정 조회는 반드시 감시자 GET /account(v3)로** — 다리를 켜면 워밍업이 클로드를 실제 호출해 구독 사용량이 나가므로, 계정 표시용으로 다리를 켜면 안 된다(검토만 쓰는 사람에게 비용 전가). 감시자는 ~/.claude.json만 읽어 비용 0. 감시자가 없으면 이미 켜진 다리에서 폴백 조회. **계정 판정은 이메일이 아니라 자격증명으로(2026-08, BRIDGE_V=39 / watcher v5)**: ~/.claude.json의 oauthAccount.emailAddress는 **로그아웃해도 남는다** — 그것만 보고 표시하면 플러그인이 로그인된 것처럼 보이고, 정작 쓰려 하면 "로그인이 필요해요"로 실패한다(실측 신고: "왜 로그인된 것처럼 표시해놓고 쓰려면 로그인하라고 하나"). 그래서 다리·감시자의 claudeAccount()가 hasClaudeCredentials()(~/.claude/.credentials.json의 claudeAiOauth.accessToken 존재)를 먼저 확인하고, 없으면 남은 이메일을 무시해 null을 준다. 파일만 읽으므로 비용 0 — claude auth status가 가장 정확하지만 조회마다 프로세스를 띄우는 건 무겁다. 검증: 모의 홈으로 (이메일 있음+자격증명 없음)→null, (둘 다 있음)→계정 확인. ⚠️ **맥은 자격증명이 파일이 아니라 키체인에 있다(2026-08, BRIDGE_V=41 / watcher v6)**: 맥의 Claude Code는 `~/.claude/.credentials.json`을 **아예 만들지 않고** 키체인 항목 `Claude Code-credentials`에 넣는다 → v39의 파일 검사만으로는 멀쩡히 로그인된 맥이 **항상** '로그인 안 됨'이 되어, 로그인 대기 화면이 영영 돌았다(실측 신고: "왜 여기서 계속 빙빙 돌까"). 이때 [재시도]를 눌러도 CLI가 "이미 로그인됨"으로 즉시 끝나(로그 `브라우저 로그인 절차 종료 (code 0)`가 5~8초마다 반복) 브라우저조차 안 뜨는 **닫힌 고리**가 된다 — 화면만 보면 '로그인이 안 되는 것'처럼 보이지만 실제로는 `claude auth status`가 loggedIn:true다. 그래서 hasClaudeCredentials는 파일이 없으면 darwin에서 키체인을 마저 본다. **존재만 확인한다(`security find-generic-password -s` — `-w` 금지)**: 비밀번호 값을 읽으면 키체인 접근 허용 팝업이 뜰 수 있다(약 30ms, launchd 감시자에서도 팝업 없이 통과 실측). `claude auth status`가 가장 정확하지만 조회마다 프로세스를 띄우는 건 여전히 무겁다. **모의 홈 테스트는 맥에서 `CB_NO_KEYCHAIN=1`을 줄 것** — 키체인은 HOME을 안 따라가서 '로그인 없음' 재현이 안 된다. **"로그인 없음"과 "확인 못 함"을 구분할 것**(source 필드) — 감시자 꺼짐을 로그인 없음으로 표시하면 멀쩡한 사용자에게 재로그인을 시킨다. **CHECK_ACCOUNT 폴백은 예외뿐 아니라 '감시자가 답을 못 준 모든 경우'에 다리로 넘어가야 한다(2026-07 버그 수정)**: 옛 감시자(v2)는 /account가 없어 404를 주는데 이건 throw가 아니라 응답이라, catch에만 폴백을 두면 안 탄다 → 다리가 켜져 있어도 계정을 못 읽어 "확인 못함"에 갇힌다(실측). res.ok && d.ok===true && 'account' in d 일 때만 감시자 확정, 아니면 watcherOld=true로 표시하고 다리 /health.account로 폴백. 로그인 후 폴링(startAccountWatch)이 이 폴백으로 계정을 잡아 화면이 갱신된다. UI 주의: 전역 `button{width:100%;height:48px !important}`가 있어 작은 버튼은 width/height/line-height를 명시적으로 되돌려야 한다(실측 — 라벨이 박스 밖으로 흐름). **[계정 전환] 버튼은 없앴다 — 로그아웃만 남긴다(2026-08 사용자 결정)**: 계정 전환을 "승인 화면 없이 로그인 화면으로" 만들려는 시도가 전부 트레이드오프에 걸렸다(시크릿 창=기본 브라우저 아님, BROWSER 가로채기=코드 붙여넣기, 로그아웃 탭 추가=탭 2개, 로그아웃만=연결 안 됨). 사용자 결론: **버튼을 없애고 [로그아웃]만 둔다** — 계정을 바꾸려면 로그아웃한 뒤 원하는 계정으로 로그인한다(브라우저 세션도 사용자가 직접 정리할 수 있어 승인 화면 문제가 사라진다). 없앤 것: 홈 계정 줄의 [계정 전환](로그인돼 있으면 버튼 자체를 숨기고, 로그아웃 상태에서만 [로그인]으로 쓴다), 추천 헤더의 [🔑], 계정 확인 팝업의 [다른 계정으로 로그인](→ 로그아웃 팝업으로), 확인 배너의 보조 버튼(→ [로그아웃]), 한도 초과 버튼 클릭(→ 안내 토스트 + 로그아웃 팝업). ui.html에 `openClaudeLogin(true)` 호출이 남아 있으면 안 된다(전환 경로 잔재 — grep으로 0개 확인할 것). 다리의 switchAccount 처리(CLI 로그아웃 후 로그인)는 코드에 남아 있지만 플러그인은 더 이상 호출하지 않는다. **계정 확인 게이트**: PC에 남은 로그인을 묻지 않고 쓰지 않는다 — 사용자가 [이 계정 사용]으로 확인한 계정(figma.clientStorage 'confirmedClaudeAccount')만 AI에 쓰고, 미확인이면 code.ts가 AI 호출을 막고(account-confirm-needed) ui.html의 .account-confirm-bar 배너(추천·번역 헤더 각 1개, 클래스 공유)가 확인을 요구한다. 계정이 바뀌면 다시 묻는다. bridge-status의 needConfirm 필드가 배너 표시/숨김을 구동. **플러그인과 생사 동기화**: code.ts가 5초마다 POST /heartbeat 전송 → 한 번이라도 받은 뒤 30초 끊기면(플러그인/피그마 종료) 다리가 스스로 꺼짐. 박동을 한 번도 못 받으면(다리만 켠 상태) 계속 대기. 종료 시 killProc이 Windows에서 taskkill /T로 **claude 트리째 정리**(shell:true라 proc.kill()은 cmd 껍데기만 죽여 고아 claude가 남을 수 있고, 이게 클로드 앱 업데이트를 "사용 중"으로 막았음). process.on('exit')에도 걸려 있어 어떤 경로로 죽든 claude가 안 남음. startProc의 close 핸들러는 `proc !== thisProc`면 무시(모델 전환 시 옛 세션 close가 새 세션을 죽이는 것 방지). manifest는 **devAllowedDomains**에 localhost 등록 — allowedDomains에 http를 넣으면 manifest 검증 실패로 플러그인이 목록에서 사라짐
- **대화형 문구 제작(2026-07, BRIDGE_V=12)**: 추천 탭 안 모드 전환([문구 다듬기/대화로 만들기], mode-toggle). 문구가 없을 때 상황을 대화로 설명하면 클로드가 문구를 만들어준다. UI가 대화 전체(composeMsgs)를 들고 있다가 매 전송마다 통째로 보냄 → code.ts COMPOSE → 다리 POST /compose → askCompose가 턴별 지시(askTranslate 패턴)로 대화 전문을 실어 요청(최근 12개만, 프롬프트 폭주 방지). 응답은 JSON {reply, suggestions[]} — 맥락 부족하면 되묻고 suggestions는 빈 배열(실측: "문구 하나 만들어줘"→"어떤 화면의 어떤 상황인가요?"). 제안은 추천과 동일하게 refineAiSuggestions 후처리, 게이트도 동일(다리 생존·계정 확인·로그인 안내). 대화 모드에선 캔버스 선택 자동채움 차단(입력창이 채팅 메시지), 다듬기 컴팩트 모드와 상호배타. **최근 대화 히스토리(2026-07)**: 대화는 figma.clientStorage('composeHistory')에 자동 저장 — 최대 10개, 대화당 메시지 40개 상한, 제목=첫 사용자 메시지 40자. **대화 모드 진입은 항상 새 대화**(2026-07 사용자 결정 — 처음엔 마지막 대화를 자동 복원했으나 "나갔다 오면 계속 예전 대화가 나온다"는 피드백으로 폐기): 모드 전환·탭 재진입 모두 composeMsgs를 비우고 시작하고, 하던 대화는 persistCompose가 이미 저장해 둬서 햄버거 메뉴 [최근 대화]에서 이어 간다. 답을 기다리다 나가도 늦은 응답은 convoId 라우팅으로 원래 대화에 저장된다. **목록 UI는 모드 탭 줄 오른쪽 끝 햄버거(#composeMenuBtn, 대화 모드에서만 표시) 드롭다운**(2026-07 사용자 결정 — 처음 만든 '첫 화면 고정 목록'과 채팅 위 [새 대화 시작] 텍스트 버튼은 위치가 어색해 폐기): [＋ 새 대화] + 최근 대화 행(제목·시간·× 삭제, 현재 대화는 '지금' 표시). 드롭다운은 **body 직속 fixed(z:1620)** — 헤더 안에 두면 헤더 스택(z:100)에 갇혀 입력 바(1500)에 가린다. 삭제는 메뉴를 열어 둔 채 갱신(연달아 삭제), 열기·새 대화·바깥 클릭·모드 전환·화면 이동은 닫는다. 저장/로드는 code.ts LOAD/SAVE_COMPOSE_HISTORY 패스스루(병합 로직은 UI가 담당, code는 목록을 통째로 읽고 쓸 뿐). 진행 중 대화 식별은 composeActiveId — 히스토리 항목을 열거나 첫 저장 때 발급되고, 새 대화·해당 항목 삭제 때 null로. **늦은 응답 라우팅**: COMPOSE에 convoId를 실어 보내고 code.ts가 compose-result에 그대로 되돌린다 — 답을 기다리다 [새 대화]/다른 대화로 바꾸면 응답을 화면이 아니라 원래 대화(히스토리 항목)에 붙여 저장만 한다(엉뚱한 대화 오염 방지, 그새 삭제된 대화면 버림). 목록 행 keydown은 e.target이 삭제(×) 버튼이면 무시(가로채면 Enter 삭제가 열기로 둔갑). **답 기다리는 표시(2026-08 사용자 요청)**: 가만히 있는 '클로드가 문구를 만드는 중…' 대신 **도는 별표 + 뒤에서 부푸는 숨결 + 빛이 흐르는 문구 + 경과 초**(composeTypingHtml / .chat-typing·.ct-orb·.ct-star·.ct-label·.ct-sec, 클로드 CLI 대기 표시와 같은 결). 경과 초는 `composeBusyStartedAt` 기준이라 renderComposeChat이 다시 그려도 이어지고, **타이머 켜고 끄기는 renderComposeChat 한 곳에서만** 한다(다시 그릴 때마다 setInterval이 쌓이면 초가 뛴다). 초 표시는 aria-hidden(1초마다 읽히면 시끄럽다), prefers-reduced-motion이면 애니메이션 없이 회색 글자로.
- **추천 화면은 칩까지만 고정, 그 아래는 스크롤(2026-08 사용자 요구)**: 예전엔 입력창·[추천받기]까지 고정 헤더(#recommendHeader) 안이라 스크롤이 버튼 아래에서만 일어났다 → 칩 아래 요소(힌트·계정 확인 배너·입력 내용·프레임 미리보기·입력창·팝업 미리보기·[추천받기])를 **#recommendBody**로 옮겨 일반 흐름에 두었다. 고정 헤더에는 [← 홈]·제목·모드 칩만 남는다(min-height 320과 콘텐츠 영역 전체로 늘리던 규칙 제거 — 그 규칙들이 버튼을 검토 화면과 같은 자리에 두던 것이라, 이제 두 화면의 버튼 위치는 일치하지 않는다). **상태 클래스는 여전히 헤더에 붙는다**(compact/chat-mode/pp-results를 토글하는 JS가 여러 곳이라 그대로 뒀다) → 옮긴 자식들의 CSS는 **형제 선택자 ** 로 읽는다. MutationObserver로 클래스를 복사하는 방식도 만들었다가 폐기했다 — 옵저버는 한 틱 늦어 토글 직후 측정이 어긋난다. showView가 #recommendBody 표시/숨김을 함께 처리해야 한다(빠뜨리면 다른 화면에 입력창이 남는다). 검증: 정적 서버로 ui.html을 띄워 스크롤 0/400에서 칩 top이 고정(97)이고 입력창·버튼·카드가 올라가는 것, compact·chat-mode·복귀 세 상태의 표시가 맞는 것을 확인. **다리 켜는 중은 화면 덮개로(2026-07, ui.html만)**: 예전엔 헤더 버튼 라벨만 '켜는 중…'으로 바뀌었는데, 그 20초 동안은 어차피 추천이 안 되므로 추천 화면 전체를 로딩 덮개(#bridgeLoading)로 덮는다. 덮개 상자(.bridge-loading-box)는 **검토 첫 화면(#topFixed.empty-review)과 같은 규격**(.title-section 타이틀 / min-height 320 / padding 20 20 12 / 안내문구는 flex:1 세로 가운데)이라 타이틀 '문구 추천받기'가 'Writing 검토하기'와 같은 자리(top 64·좌 20), [클로드 켜기]가 [검토하기]와 같은 자리(top 304·h 48·좌우 20)에 온다. **덮개가 헤더를 가리므로 타이틀은 덮개 안에 다시 넣어야 한다** — 안 넣으면 추천 화면인데 이름이 사라진다 — 한쪽 규격을 바꾸면 탭을 오갈 때 버튼이 튀므로 같이 고칠 것. 버튼은 button-primary/secondary를 그대로 쓰고 라벨에 이모지를 넣지 않는다(사용자 요구). stopped에는 예시 그래픽(.bridge-loading-art, 플러그인 카드 ↔ 클로드 별표를 점선으로 잇는 그림)도 넣어 검토 첫 화면과 짝을 맞춘다 — **아래 여백을 키우면 상자가 320을 넘겨 버튼이 검토 화면보다 아래로 밀리니 주의**. **사용자 문구는 '켜다/다리'가 아니라 '연결/연동'으로 통일(2026-07 사용자 결정)** — 상태 배지 '🟢 클로드 연동 · 계정' / '⚫ 클로드 연동 안 됨', 토스트 '클로드가 연결됐어요', 동작 '클로드 연결하기'·'연결 끊기'. '다리(bridge)'는 코드·주석에만 남기고 화면에 노출하지 않는다. '연결'은 동작 명사라 '연결해 주세요'로 띄어 쓰고, '연결되었어요'는 검토기가 '연결됐어요'로 고치라 하므로 **됐어요** 쪽을 쓴다. **덮개 문구도 이 플러그인의 검토를 통과해야 한다** — vm 샌드박스로 suggestFriendlyKorean(문구)가 빈 배열인지 확인할 것(실제로 마침표 누락 3건, '처음이면' 오탐 1건이 잡혀 고쳤다). 탭 바 z-index를 1600으로 올려 덮개(1550)·대화 입력 바(1500) 위에 두었으므로 **로딩 중에도 검토/홈으로 나갈 수 있다**(토스트는 1700). 덮개 상태는 3종(bridgeLoadState) — `waking`(로딩) / `failed`(20초 안에 못 켬 → [🔌 클로드 켜기]+[🔧 설치 파일 받기]+[클로드 없이 추천받기]) / `stopped`(사용자가 헤더 버튼으로 직접 꺼둠 → [🔌 클로드 켜기] **하나만**). **stopped는 닫을 수 없다** — 추천은 클로드가 하는 일이라 끈 채로는 화면을 쓸 이유가 없다는 판단(사용자 결정, 2026-07). 반면 failed는 켤 방법 자체가 막힌 상황이라 [클로드 없이 추천받기]로 빠져나가 예시·규칙 폴백을 쓸 수 있게 남겨 둔다. stopped는 **끄는 즉시** 전환된다(bridge-status에서 `!alive && userTurnedOff`) — 탭 재진입을 기다리면 끈 직후엔 멀쩡해 보이는 화면에서 추천만 안 되는 것처럼 보인다. **다리가 꺼져 있는데 추천 화면에서 아무것도 안 뜨는 경우는 없어야 한다**: `userTurnedOff`일 때 ensureBridgeFromGesture가 그냥 return하던 탓에 탭을 왔다 갔다 해도 화면이 조용했던 버그가 있었음. 실패는 `bridgeWakeFailed`로 기억해 **들어올 때마다 또 20초를 태우지 않고** 곧장 조치 화면을 보여준다(다리가 살아나면 두 플래그 모두 해제되고 덮개도 스스로 걷힌다). 추천 탭으로 전환하면 검토 결과·캔버스 초록 코멘트를 resetReview()로 초기화한다.
- **로그인은 항상 '계정 선택'으로 열 것(2026-07 버그 수정)**: 홈 계정 줄 버튼이 `openClaudeLogin(!!knownAccount)`라서, 로그인된 계정이 없을 때(게스트·미로그인) switch=false로 열렸고 → 브라우저에 claude.ai 세션이 남아 있으면 **계정 선택이 아니라 승인 화면으로 직행**했다(실제 신고). 계정 화면의 accountSwitch는 같은 이유로 이미 true 고정이었음 — 홈 버튼도 true 고정으로 맞췄다. `bridgeWakeBtn`의 claude-logout 분기만 false(만료 재로그인 = 같은 계정)로 남겨 둠.
- **`/open-login`은 claude 유무를 먼저 확인할 것(2026-07 버그, BRIDGE_V=13)**: 예전엔 확인 없이 `spawn('claude', ...)` 후 곧바로 `ok:true`를 돌려줬다. **shell:true라 claude가 없어도 셸은 정상 실행돼 spawn의 'error'가 안 뜬다** → 플러그인은 "브라우저를 열었어요"라 하는데 실제로는 아무 창도 안 뜨는 상태(다른 PC에서 실제 신고). 이제 `claudeStatus==='claude-missing'`이면 501로 끊고, 시동 점검이 끝나기 전 클릭한 경우를 위해 **로그인 프로세스가 5초 안에 실패로 끝나면 checkClaudeAvailable()을 다시 돌려** /health로 알린다(UI가 대기 화면을 실패로 전환). 재현: PATH에서 claude를 뺀 fakebin + BRIDGE_PORT로 별도 기동.
- **팝업 요소별 추천은 "세트(케이스)" 단위로(2026-07, BRIDGE_V=15)**: 텍스트 2개 이상 든 컴포넌트(팝업)를 선택하면 code.ts `popupRecommendFlow`가 `classifyPopup`으로 역할(타이틀/안내/버튼(주요)/버튼(일반))을 나눠 **팝업 전체를 한 요청**(다리 `POST /recommend-popup` → `askPopup`)으로 보낸다. **요소를 하나씩 따로 보내면 타이틀이 본문 맥락을 못 봐서(타이틀만 봐도 알게 하려면 본문 필요) 안 되고**, 응답도 요소별 낱개 3개가 아니라 **타이틀·안내·버튼이 일관된 완성 세트 2~3개**(`{sets:[{reason, elements:[{role,text}]}]}`)로 받는다 — 낱개로 뽑아 조합하면 서로 어긋나기 때문(사용자 지적). 세트 문구는 ux-writing.md "8. 팝업" 규칙을 따른다(타이틀 명사형·종결어미 없음, 버튼은 통보=[확인]/판단=[아니오]·[네]/동작=[취소]·[동작], "닫기 통일·취소 금지" 옛 규칙 폐기). 후처리는 refinePopupSets(용어집+맞춤법, **중복 제거는 안 함** — 세트 간 같은 버튼 문구가 겹쳐도 세트를 온전히 유지). UI renderPopupRecommend는 세트 카드로 렌더(각 줄 클릭=복사), [초기화]는 '팝업 문구 N개 감지' 미리보기 헤더 오른쪽. **[다른 케이스 더 받기](2026-08, BRIDGE_V=20)**: 한 번에 2~3개만 오니 결과 맨 아래 버튼으로 세트를 더 받는다(다듬기 쪽 [AI 추천 더 받기]와 같은 역할·같은 button-secondary/full-width). 재요청은 **캔버스 선택이 아니라 code.ts가 기억한 `lastPopupElements`로** — 결과를 보는 동안 선택이 풀리거나 다른 프레임으로 바뀔 수 있다(UI → `RECOMMEND_POPUP_MORE` → popupRecommendFlow(opts.elements/append)). 다리는 요청 body의 `more`(+askPopup의 askedCount)로 "앞서 낸 세트와 겹치지 않는 새 세트"를 요구한다 — 안 걸면 클로드가 같은 세트를 또 낸다(askClaude와 같은 이유). 결과는 기존 카드를 지우지 않고 아래에 덧붙이고 '케이스 N' 번호를 이어 붙이며, 이미 화면에 있는 세트와 같은 것은 DOM 지문(popupSetSigs)으로 걸러 토스트로 알린다(별도 상태를 안 두는 이유: [초기화]가 목록을 비우면 지문도 같이 사라져 어긋나지 않는다). 실패해도 기존 카드는 유지(append면 토스트만) + `popup-more-end`로 버튼을 원복 — 다리 꺼짐 같은 조기 반환 경로엔 hide-loading이 없어 이 신호가 없으면 버튼이 '케이스 받는 중…'에 갇힌다.
- **하위 프레임별 문구 묶음(2026-08, 다듬기)**: 화면·섹션처럼 '프레임 안에 프레임이 여럿'인 걸 누르면 문구가 한 덩어리로 입력창에 들어와 어느 영역 것인지 알 수 없었다 → code.ts `classifyFrameGroups`가 하위 프레임별 `[{name, texts}]`로 나눠 selection-text의 `groups`로 실어 보내고, ui.html `renderFramePreview`가 입력창 **위**에 미리보기 카드로 보여준다(팝업 미리보기는 입력창을 대체하지만 이건 얹힌다 — 입력창이 실제 추천 대상이라서). 그룹 카드를 누르면 그 그룹 문구만 입력창에 들어가 부분만 다듬고, [전체]로 되돌린다(`selectFrameGroup`, -1=전체). 설계 요점: ① **끝까지 내려가 '문구를 직접 담은 프레임' 단위로 나눈다**(2026-08 수정) — 처음엔 '갈라지는 첫 층'만 그룹으로 썼더니 그 층 안에서 또 나뉜 프레임들의 문구가 한 그룹으로 합쳐져 **"다른 프레임인데 같은 프레임으로 나온다"는 신고**가 있었다. 이름은 조상 프레임 이름을 이어 `헤더 › 타이틀` 경로로 만들고, 뻔한 래퍼 이름(Frame 12·Auto layout·Group·Container…)은 경로에서 뺀다(isPlainName — 안 빼면 'Auto layout › 헤더'처럼 읽힌다). 프레임이 자기 문구와 하위 프레임을 같이 들면 자기 문구만 자기 그룹(하위는 각자) — 이때 그 그룹엔 **`own:true`를 실어 UI가 '이 프레임 문구' 칩을 붙인다**(2026-08): 안 붙이면 자식 그룹들 사이에 상위(또는 선택한) 프레임 이름이 섞여 "특정 프레임을 눌렀는데 왜 상위 프레임도 같이 뜨지?"가 된다(실제 신고). own은 `anyTextInside`로 판정 — 하위에 문구가 아예 없으면(자식 그룹이 안 생기면) 칩을 안 붙인다. UI는 미리보기·결과 양쪽에 같은 칩을 쓰므로 `RECOMMEND_GROUPS`/`sent`에도 own을 함께 실어 보낸다. 상한은 깊이 12단·그룹 60개 ② 그룹이 2개 미만이면 빈 배열 → 미리보기 없이 기존 동작 ③ 숨긴 노드 제외(findAllTextNodes와 같은 기준 — 입력창 문구와 어긋나면 안 된다) ④ 순서는 레이어가 아니라 화면 위→아래 ⑤ 여러 개를 한꺼번에 고르면 그룹핑 안 함(기준 프레임이 없음) ⑥ 미리보기 표시는 인라인 display로 켜고 끄므로 **CSS로 숨길 수 없다** — 팝업 미리보기와 같이 setRecommendCompact(결과 화면)·setRecommendMode(대화 모드)에서 JS로 접고 복원한다 ⑦ 그룹을 고르면 `lastSelectionText`도 같이 갱신 — 안 그러면 다음 선택 조회가 '사용자가 직접 고친 문구'로 보고 덮지 않는다. **[전체]로 추천받으면 영역마다 따로 결과(2026-08, BRIDGE_V=21)**: 한 덩어리로 다듬으면 화면 전체가 한 문구처럼 섞여 나온다(사용자 지적) → UI가 `RECOMMEND_GROUPS`(frameGroups 그대로)로 보내고 code.ts `groupsRecommendFlow` → 다리 `POST /recommend-groups`(`askGroups`)가 **한 요청에 모든 영역을 실어** 영역별 대안 2개씩을 받는다(`{groups:[{name, suggestions}]}`). 영역 수만큼 요청을 쪼개지 않는 게 핵심 — 그만큼 느려지고 구독 사용량도 그만큼 나간다. 프롬프트는 영역별 **같은 줄 수** 유지를 요구하고, 고칠 게 없으면 빈 배열을 허용한다(UI는 '고칠 게 없다고 봤어요'). 응답의 name은 신뢰하지 않고 우리가 보낸 이름을 쓴다(모델이 바꿔 적는다). 제안은 refineAiSuggestions로 후처리. 영역 상한 10개(MAX_RECOMMEND_GROUPS) — 넘으면 자른 사실을 토스트로 알린다(조용히 빼먹지 않는다). 결과 렌더(renderGroupsRecommend)는 renderRecommend와 같은 sugg-card 구조라 좋아요·복사 배선(wireLikeButtons/wireCopyButtons)을 공유하고, 헤더엔 팝업 결과처럼 `pp-results`를 붙여 미리보기를 남긴다 — 그래서 결과를 보다가 특정 영역을 다시 고를 수 있고, 그때는 대상이 달라지므로 `clearGroupResults()`로 전체 결과를 비운다. 조기 반환(다리 꺼짐·계정 확인)엔 hide-loading이 없어 `groups-recommend-end`로 버튼을 원복해야 '영역마다 다듬는 중…'에 갇히지 않는다.
- **버튼을 고르면 버튼으로 알아본다(2026-08, BRIDGE_V=22)**: 팝업 안에서만 역할을 갈랐던 탓에 버튼 하나를 고르면 그냥 텍스트로 다뤄 문장형 대안('확인해 주세요')이 나왔다(사용자 지적) → code.ts `detectButtonRole`이 이름(button/btn/버튼/cta/action, 자기+위로 3단)이나 '배경 있는 작은 컴포넌트(≤80px) 안의 텍스트'로 버튼을 판정하고, selection-text의 `role`·프레임 묶음의 `group.role`로 실어 보낸다. **오탐 가드가 핵심**: 문구 2개 이하 + 각 14자 이하 + 문장부호로 끝나지 않을 때만 버튼으로 본다 — 없으면 'CTA 카드' 같은 이름의 섹션 본문이 버튼 규칙으로 다듬어진다. UI는 힌트·그룹 카드·결과에 `.role-chip`('버튼')을 띄우고 RECOMMEND/RECOMMEND_GROUPS에 role을 실어 보낸다(그룹을 골랐으면 그 그룹의 role). 다리는 `BUTTON_RULE`(마침표·물음표·종결어미 금지, 짧은 동작 명사, 통보성 단일 버튼은 '확인')을 askClaude·askGroups 프롬프트에 얹는다. 그래도 모델이 문장형을 섞어 내므로(실측 '확인했어요') code.ts `refineButtonSuggestions`가 끝 문장부호를 떼고 **종결어미(요·다·까)로 끝나는 제안을 버린다 — 2개 이상 남을 때만**(다 버려 빈손이 되는 것보다 낫다). '네'·'아니오'는 길이·끝글자 조건에서 안 걸린다.
- **오류 토스트가 덮개에 가리지 않게 할 것**: 토스트 z-index는 2700 — 로그인 대기(2600)·클로드 로딩(1550)보다 위여야 한다. 낮추면 실패 안내가 화면 뒤에 깔려 "아무 반응 없음"이 된다(실제 발생). 더 확실하게는 `login-open-result`처럼 **구조화된 결과 메시지**를 보내 덮개 자체가 상태를 바꾸게 한다.
- **로그인 대기엔 로딩을 띄울 것(#loginWaiting)**: 브라우저 OAuth가 도는 동안 플러그인은 폴링만 해서 죽은 것처럼 보였다(토스트는 몇 초 뒤 사라져 아무 표시도 안 남음) → 계정이 잡힐 때까지 전체 화면 로딩. **걷는 조건은 '계정이 있음'이 아니라 '계정이 바뀜'(loginWaitFromAccount 비교)** — 계정 전환은 새 로그인이 끝나기 전까진 ~/.claude.json에 옛 계정이 그대로라 단순 존재 검사로는 즉시 걷혀 버린다. 확인 팝업이 뜰 때도 걷고(팝업을 가리면 안 됨), 2분 폴링이 끝나도 안 잡히면 걷고 안내한다(영영 도는 로딩 금지). [나중에 하기]는 폴링까지 멈춘다 — 안 그러면 나중에 팝업이 불쑥 뜬다.
- **로그아웃 직후 낡은 계정 조회를 무시할 것(2026-07 버그)**: 로그아웃하면 로그인 화면으로 가며 requestAccountInfo를 부르는데, **감시자 /account는 5초 캐시**라 방금 로그아웃한 계정을 아직 돌려줄 수 있다 → decideFirstScreen이 "로그인됨"으로 읽어 홈으로 튕기고 확인 팝업까지 띄웠다(실제 신고). ui.html에 `justLoggedOut` 플래그 — 로그아웃 성공 시 set(+stopAccountWatch+열린 팝업/배너 닫기), account-info가 **계정 있음이면 무시(로그인 화면 유지), null이면 해제**, openClaudeLogin에서 해제(재로그인은 진짜). maybeAskConfirmAccount·showAccountConfirm도 justLoggedOut·`currentView==='account'`이면 안 뜬다.
- **실패 안내는 OS로 갈릴 것(2026-07)**: 설치 `.bat`은 **윈도우 전용**(자기완결 payload). 맥은 그런 게 없고 **`npm install` 한 번**이 설치 과정(postinstall→register-protocol.js가 launchd 감시자 등록·기동). 그래서 로그인/연결 실패 화면의 [설치 파일 받기] 버튼은 `body.is-mac`에서 숨기고(안내문도 "폴더에서 npm install 실행"으로) 표시한다. IS_MAC은 ui.html이 navigator.platform/userAgent로 판정(피그마 데스크톱=Electron이라 신뢰 가능). **맥도 윈도우와 같은 방식으로 통일(2026-07 최종)** — 설치 버튼(accountInstall·loginWaitingInstall·bridgeLoadingInstall) → GET_INSTALLER(msg.mac) → 다운로드 → 한 번 실행. 터미널 한 줄(curl|bash) 방식은 만들었다가 **사용자 결정으로 제거**(맥·윈도우 경험을 같게). 다만 build는 여전히 옆 폴더 서버 저장소에 api/bridge-setup.js를 내보낸다 — 플러그인은 안 쓰고, IT 배포나 수동 설치용 옵션으로만 남김. 처음 PC 감지(감시자 무응답 = accountSource가 watcher/bridge가 아님)면 **첫 로그인 화면(accountView)에 설치 버튼을 바로** 보여준다(실패를 겪기 전에). 설치 파일 상세: **버튼 → 다운로드 → 한 번 실행**: build가 자기완결형 **클로드-커넥터.command**를 생성(다리·감시자·예시·가이드를 base64 payload로 내장, `exit 0` 뒤 ::BRIDGE::/::WATCHER::/::EXAMPLES::/::GUIDE::/::END:: 섹션, awk 매개변수 추출이라 마커 자기매칭 없음)하고, 이를 **+x(0755)를 외부 속성에 실은 단일 항목 zip**(build-glossary.js의 자체 zip 라이터 — 무압축 stored)으로 감싸 code.ts INSTALLER_MAC 마커에 주입한다. **왜 zip인가: 브라우저 다운로드는 유닉스 실행 권한을 못 날라 받은 .command가 더블클릭 거부됨** — zip 외부 속성은 압축 해제 때 복원된다(ditto로 실측). **격리(quarantine)는 zip→압축해제 파일로 상속되는 것이 실측 확인됨** — 그래서 맥은 첫 실행이 반드시 **우클릭 → [열기]**(더블클릭은 Gatekeeper가 막음). 없애려면 Apple 개발자 서명·공증이 필요. GET_INSTALLER는 msg.mac으로 갈려 맥엔 클로드-커넥터.zip을 준다. 실행하면 ~/Library/Application Support/ClaudeBridge에 설치 + launchd plist(절대경로 node + PATH 굳힘) 등록·기동 + node/claude 점검. 루트의 .command는 **빌드 산출물 — 직접 수정 금지**(bat과 동일). Gatekeeper 때문에 받은 파일은 우클릭→[열기] 한 번 필요. **"플러그인이 백단에서 몰래 설치"는 불가능** — 샌드박스는 fetch만 되고 디스크 쓰기/실행이 없다. 한 번의 파일 실행이 물리적 최소치. **맥에서 깃 zip만 받아 아무 설치 없이 플러그인을 열면 로그인이 안 되는 게 정상** — 감시자가 없어 다리를 못 깨운다.
- **코멘트는 초록 말풍선(createCommentFrame)이 최종** (2026-07 확정). 네이티브 어노테이션(node.annotations) 프로토타입은 시도 후 폐기 — 잔재 코드도 정리됨(복구는 git 히스토리). 크기 배지를 숨기는 API는 없음(확정) — 클릭 직후 selectionchange 핸들러가 선택을 비워 배지가 안 뜨게 처리

## 서버 구조 (2026-07 이사)

- 심부름꾼 서버는 **Vercel 제보 앱(ux-writing-reports 저장소, report-admin-amber.vercel.app)에 통합** — 플러그인이 쓰는 건 `GET /api/passport`(맞춤법 열쇠)와 제보(/report·좋아요)뿐. `POST /api/recommend`·`POST /api/translate`는 서버에 남아 있지만 플러그인은 더 이상 호출 안 함(클로드 다리 전용 전환). 이사 이유: 사내 보안 프록시가 workers.dev를 '1회성 사용' 안내 페이지로 가로채 플러그인 fetch가 `Failed to fetch`로 실패 (vercel.app은 통과)
- 구 Cloudflare Worker(naver-passport-proxy/)는 **2026-07 삭제** (Vercel 이사 후 미사용 확인) — 롤백이 필요하면 git 히스토리에서 복구: code.ts NAVER_PROXY_URL을 워커 주소로 바꾸고 passport 경로 결합부 수정, manifest allowedDomains에 workers.dev 재추가
- `npm run build`는 recommend-examples.md를 code.ts + **옆 폴더의 ux-writing-reports/api/recommend.js**(있을 때만)에 주입 — 주입 후 그쪽 저장소에서 커밋+푸시해야 실서버 반영
- 서버 배포 = ux-writing-reports 저장소에 git push (Vercel 자동 배포). vercel.com 대시보드는 사내에서 차단이라 안 열리지만 배포에는 불필요

## 주의

- 검토 시 텍스트가 외부(Vercel 서버 + 네이버)로 전송됨 — 서버 주소는 code.ts의 NAVER_PROXY_URL, manifest.json allowedDomains와 함께 변경
- 네이버 SpellerProxy는 비공식 API (passportKey 방식) — 깨지면 로컬 규칙만으로 동작
